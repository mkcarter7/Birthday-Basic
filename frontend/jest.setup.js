/**
 * Jest global setup — runs once after the test framework is loaded
 * but before any test files execute.
 *
 * @testing-library/jest-dom adds extra matchers like:
 *   expect(element).toBeInTheDocument()
 *   expect(element).toHaveTextContent('hello')
 * These make DOM assertions much more readable than comparing raw HTML strings.
 */
import '@testing-library/jest-dom';
